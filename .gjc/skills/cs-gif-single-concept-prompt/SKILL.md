---
name: cs-gif-single-concept-prompt
description: 단일 개념용 학습 GIF를 만들 때 그대로 복붙해 쓰는 프롬프트 템플릿입니다. 스택, 큐, 힙, DFS, DP 등 한 개념의 동작 원리를 설명보다 움직임으로 이해시키는 GIF를 요청해야 할 때 사용합니다.
use_conditions: 한 개념만 정확하게 GIF로 만들고 싶고, 텍스트 설명은 최소화한 채 상태 변화와 핵심 동작을 직관적으로 보여줘야 할 때.
---

# CS GIF Single Concept Prompt

아래 템플릿을 그대로 복붙해서 쓰면 됩니다.

## Copy-paste template

```text
<개념명>을 설명하는 학습용 GIF를 만들어줘.

요구사항:
- 설명문보다 **움직임만 보고 작동 원리가 직관적으로 이해되게** 만들어.
- 텍스트/자막은 최소화.
- 한 번 보면 “아, 이렇게 동작하는구나”가 바로 와야 해.
- 정적인 인포그래픽 말고 실제 **looping GIF**로 만들어.
- 핵심 상태 변화가 분명히 보여야 해. (예: push/pop, enqueue/dequeue, 탐색 순서, split, swap, relax 등)
- 모바일/위키 본문 폭에서도 식별 가능하게 만들어.
- active 요소는 색상/강조로 분명히 보여줘.
- 결과물은 실제 `.gif` 파일로 만들고, 최종 연결은 서버 URL로 하도록 준비해.

의도:
- 시험/면접용 학습 자료라서 긴 설명보다 동작 구조를 한눈에 이해시키는 게 목적이야.
- “설명”이 아니라 “상태 변화 시각화”에 집중해.

예시 톤:
- 스택이면 위에서 쌓이고 위에서 빠지는 흐름
- 큐면 앞에서 빠지고 뒤에서 들어오는 흐름
- DFS면 깊게 들어갔다가 되돌아오는 흐름
- 힙이면 루트 제거 후 아래로 재정렬되는 흐름
```

## Fill-in helper

- `<개념명>` 자리에 예: `스택`, `큐`, `연결리스트 삽입`, `힙 extract-max`, `DFS`, `B트리 split`

## Good request examples

- `스택을 설명하는 학습용 GIF를 만들어줘.`
- `힙에서 extract-max 후 재정렬되는 과정을 설명하는 학습용 GIF를 만들어줘.`
- `DFS가 깊게 들어갔다가 backtracking하는 흐름을 보여주는 GIF를 만들어줘.`
